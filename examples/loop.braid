def decr = _ 1 >> -
def sumStep = (a n -> n >> zero? >> ((z -> a >> done) | (m -> (a m >> +) (m >> decr) >> again)) >> merge)
def sumTo = [sumStep] ... >> loop

0 5 >> sumTo >> print

0 4
[(a n -> n >> if
... | [(z -> a >> done)] zero?
elif
... | [(m -> (a m >> +) (m >> decr) >> again)] otherwise
endif)] ...
loop
print
